# Flip A Coin

Roblox project built with Rojo.

## Start

```bash
rojo serve
```

Then open `default.project.json` in Roblox Studio or build a place file with Rojo when needed.

## Docs

- `docs/BOOTSTRAP.md` for startup routing
- `docs/TASK_STATE.md` for current work
- `docs/PROJECT_LOGIC.md` for project facts
- `docs/FRAMEWORK.md` for framework rules
