---
description: Excel to Lua 转换工具的架构与使用规范
globs: Excels/**/*.py
alwaysApply: false
---

# Excel to Lua 转换工具 (ExcelToLua3.py)

## 工具概述

`Excels/ExcelToLua3.py` 是项目的配置数据管线工具，将策划维护的 `.xlsx` Excel 表自动转换为 Luau `table.freeze(...)` 只读配置模块，输出到 `src/ReplicatedStorage/ExcelConfig/`。

## 运行模式

| 模式 | 触发条件 | 行为 |
|------|---------|------|
| **批处理** | 不传 `--input` 参数 | 扫描 `Excels/` 目录下所有 `.xlsx`，每个 Sheet 导出一个 `.lua` |
| **单文件** | 传 `--input path.xlsx` | 转换指定文件的指定 Sheet |

```bash
# 批处理（最常用）
cd Excels && python ExcelToLua3.py

# 单文件
python ExcelToLua3.py --input Blocks.xlsx --output out.lua --sheet Sheet1
```

## Excel 表格约定格式

支持两种表头布局（自动探测）：

```
布局A（类型在下方）:          布局B（类型在上方）:
第1行: 显示名/中文注释         第1行: 类型行（string/int/table...）
第2行: 表头行（字段名）         第2行: 显示名/中文注释
第3行: 类型行                  第3行: 表头行（字段名）
第4行起: 数据行                第4行起: 数据行
```

- 以 `#` 开头的列为设计师注释列，自动跳过
- 空单元格默认输出 `nil`（保留字段）
- 主键列优先查找名为 `id` 的列（不区分大小写）
- 表头支持点号嵌套路径（如 `loot.Stone`）

## 支持的类型

| 类型声明 | 转换规则 |
|---------|---------|
| `string` / `str` | 强制字符串 |
| `int` / `integer` | 强制整数 |
| `float` / `number` | 强制浮点 |
| `bool` / `boolean` | 强制布尔（true/false） |
| `table` | 解析 table 文本语法 |
| 无声明 | 自动推断（数字/布尔/nil/字符串） |

## Table 文本语法

单元格中 table 类型支持两大类格式：

### 无花括号格式（`|` 为顶层分隔符）
```
"1|2|3"                    -> { 1, 2, 3 }
"key=value"                -> { key = value }
"a:b:c"                    -> { a = { b = c } }
"id=10,count=3|id=11,count=5" -> { {id=10,count=3}, {id=11,count=5} }
```

### 花括号格式（`,` 为顶层分隔符，兼容 Excel 内策划直接写的类 Lua 语法）
```
"{Stone=0.9,Coal=0.07}"        -> { Stone = 0.9, Coal = 0.07 }
"{[1]=0.9,[2]=0.1}"            -> { [1] = 0.9, [2] = 0.1 }
"{Coal=0.07,\nCopper=0.02}"   -> { Coal = 0.07, Copper = 0.02 }（支持多行）
```

- 花括号模式：外层 `{}` 自动剥离，逗号为主分隔符，换行视为逗号
- `[N]=value` 方括号数字键会输出为 Lua 的 `[1] = 0.9` 格式
- `=` 或 `:` 分隔键和值
- `:` 多级嵌套路径 `a:b:c` -> `{ a = { b = c } }`

## 全局配置常量

在脚本顶部的全局变量控制批处理行为：

| 变量 | 默认值 | 说明 |
|------|-------|------|
| `AUTO_DETECT_LAYOUT` | `True` | 自动探测表头/类型/数据行位置 |
| `ROOT_KEY_MODE` | `"none"` | 根键模式：none/sheet/excel/excel+sheet |
| `FREEZE_RETURN` | `True` | 输出 `table.freeze(...)` |
| `OUTPUT_DIR` | `"../src/ReplicatedStorage/ExcelConfig"` | 输出目录（相对 Excels/） |
| `OUTPUT_NAME_MODE` | `"excel+sheet"` | 文件名 = Excel文件名 + Sheet名 |
| `SKIP_SHEETS` | `{"注释"}` | 跳过的 Sheet 名称 |
| `FORCE_NIL_FIELDS` | `{"key", "productid"}` | 空值时仍输出 nil 的字段 |

## 核心架构（函数流水线）

```
main()
 ├─ 批处理: _convert_workbook_batch()
 │   ├─ _detect_layout()         # 自动探测行1-3的布局
 │   ├─ build_table_from_worksheet()  # 核心：worksheet -> Python dict
 │   │   ├─ _parse_headers()     # 解析表头路径和类型声明
 │   │   ├─ _resolve_key_column()# 确定主键列
 │   │   ├─ _parse_cell()        # 按类型解析每个单元格
 │   │   │   └─ _parse_table_text()  # table 类型专用解析
 │   │   └─ _set_path()          # 将值写入嵌套字典路径
 │   └─ _to_lua()                # Python dict -> Lua 源码字符串
 │       ├─ _lua_key()           # 格式化 Lua 表键
 │       ├─ _lua_string()        # 格式化 Lua 字符串字面量
 │       └─ _format_number()     # 格式化 Lua 数字字面量
 └─ 单文件: convert_excel_to_lua()
     └─ (同上流程)
```

## 修改指南

- **新增类型**：在 `KNOWN_TYPES` 集合和 `_parse_cell()` 中添加分支
- **修改输出格式**：调整 `_to_lua()` 的递归序列化逻辑
- **修改布局探测**：调整 `_detect_layout()` 的判断策略
- **新增 table 语法**：在 `_parse_table_text()` 中添加解析分支
- **调整批处理行为**：修改脚本顶部的全局配置常量

## 依赖

- `openpyxl`：读取 `.xlsx` 文件（`pip install -r requirements.txt`）
